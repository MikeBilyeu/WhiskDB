import React from "react";

const Categories = () => {
  /*
  pass in categories as prop from parent

  keep track of categories on local state in arr
  map over local catArr render ul
  li button onClick this.props.fields.push() and setState local

  render ul
  this.props.fields.map
  li button onClick this.props.fields.remove(index) and setState local



  */
  return <div></div>;
};

export default Categories;
